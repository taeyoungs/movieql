type Movie {
  id: Int!
  title: String!
  score: Int!
}

type Query {
  movies: [Movie]!
  movie(id: Int!): Movie!
}

type Mutation {
  addMovie(title: String!, score: Int!): Movie!
  deleteMovie(id: Int!): Boolean!
}
